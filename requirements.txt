# FastAPI Framework
fastapi[all]

# ASGI Server
uvicorn[standard]  # ASGI server for production

# Database (SQLAlchemy, Alembic, asyncpg)
sqlalchemy[asyncio]  # SQLAlchemy ORM with async support
asyncpg  # PostgreSQL async driver
alembic  # Database migrations

# Redis (for caching and task queues)
redis  # Redis client for Python
aioredis  # Async Redis library

# Task Queue (Celery + Redis backend)
celery[redis]  # Celery for background task management
flower  # Celery task monitoring dashboard

# Authentication and Security
python-jose[cryptography]  # JWT generation and verification
passlib[bcrypt]  # Password hashing utilities
pyotp  # Time-based One-Time Passwords (for 2FA)
argon2-cffi  # Advanced password hashing

# GraphQL
strawberry-graphql  # GraphQL integration with FastAPI

# Monitoring and Tracing
prometheus-client  # Prometheus metrics exporter
opentelemetry-api  # OpenTelemetry API for tracing
opentelemetry-sdk  # OpenTelemetry SDK
opentelemetry-instrumentation-fastapi  # OpenTelemetry instrumentation for FastAPI

# Search Functionality
elasticsearch  # Elasticsearch client
opensearch-py  # OpenSearch client

# Testing
pytest  # Testing framework
pytest-asyncio  # Async support for pytest
httpx  # Async HTTP client for testing

# Logging and Observability
structlog  # Advanced structured logging
loguru  # Simplified logging framework
sentry-sdk  # Sentry for error tracking

# Caching
aiocache  # Distributed caching for async apps
diskcache  # File-based cache for persistent storage

# API Rate Limiting
slowapi  # Rate-limiting middleware for FastAPI

# Job Scheduling
apscheduler  # Advanced job scheduling

# Internationalization (i18n)
babel  # Internationalization and localization utilities

# Other Utilities
python-dotenv  # Environment variable management
orjson  # Ultra-fast JSON handling
pydantic[email]  # Pydantic email validation
pillow  # Image processing
shortuuid  # Generate short unique IDs
phonenumbers  # Phone number parsing and validation
aiobotocore
boto3
python-multipart
pymagento
elasticsearch_dsl[async]
fastapi-events
Jinja2
PyJWT
marshmallow-sqlalchemy
Faker
pandas
pusher
openpyxl
ShopifyAPI
nylas
dramatiq
jsonpath_rw
jsonpath-ng
requests-oauthlib
kaiba
pinecone
more-itertools
pdfkit-async
panzi-json-logic
pysftp
mixpanel
